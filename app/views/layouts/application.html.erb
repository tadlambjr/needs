<!DOCTYPE html>
<html lang="en" data-controller="theme">
  <head>
    <script>
      // Set theme before page renders to prevent flash
      const theme = localStorage.getItem('theme') || 'system';
      if (theme === 'dark') {
        document.documentElement.classList.add('dark');
      } else if (theme === 'system' && window.matchMedia('(prefers-color-scheme: dark)').matches) {
        document.documentElement.classList.add('dark');
      }
    </script>
    <title><%= content_for(:title) || "Church Needs" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-100 min-h-screen">
    <% if authenticated? %>
      <%= render "shared/navigation" %>
    <% end %>

    <main class="<%= authenticated? ? 'pt-16' : '' %>">
      <%= render "shared/flash_messages" if notice.present? || alert.present? %>
      
      <% if content_for?(:container_class) && content_for(:container_class).blank? %>
        <%= yield %>
      <% else %>
        <div class="<%= content_for?(:container_class) ? yield(:container_class) : 'container mx-auto px-4 py-8' %>">
          <%= yield %>
        </div>
      <% end %>
    </main>

    <%= render "shared/footer" unless content_for?(:hide_footer) %>
  </body>
</html>
