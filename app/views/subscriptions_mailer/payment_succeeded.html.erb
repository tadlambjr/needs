<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #10b981; color: white; padding: 20px; text-align: center; border-radius: 8px 8px 0 0; }
    .content { background-color: #f9fafb; padding: 30px; border-radius: 0 0 8px 8px; }
    .button { display: inline-block; background-color: #0ea5e9; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; margin: 20px 0; }
    .footer { text-align: center; margin-top: 30px; color: #6b7280; font-size: 14px; }
    .receipt { background-color: white; padding: 20px; border-radius: 6px; margin: 20px 0; }
    .receipt-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #e5e7eb; }
    .receipt-row:last-child { border-bottom: none; font-weight: bold; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Payment Received ✓</h1>
    </div>
    
    <div class="content">
      <p>Hi <%= @owner.name %>,</p>
      
      <p>Thank you! We've successfully received your payment for ChurchNeeds.net.</p>
      
      <div class="receipt">
        <h3 style="margin-top: 0;">Payment Receipt</h3>
        <div class="receipt-row">
          <span>Amount Paid:</span>
          <span>$<%= sprintf("%.2f", @amount) %></span>
        </div>
        <div class="receipt-row">
          <span>Date:</span>
          <span><%= Time.current.strftime("%B %d, %Y") %></span>
        </div>
        <div class="receipt-row">
          <span>Billing Period:</span>
          <span>Yearly</span>
        </div>
        <% if @next_payment_date %>
        <div class="receipt-row">
          <span>Next Payment:</span>
          <span><%= @next_payment_date.strftime("%B %d, %Y") %></span>
        </div>
        <% end %>
      </div>
      
      <p>Your continued support helps us maintain and improve ChurchNeeds.net for churches everywhere. We're grateful for your partnership!</p>
      
      <div style="text-align: center;">
        <a href="<%= manage_donations_url %>" class="button">View Subscription Details</a>
      </div>
      
      <p>Blessings,<br>The ChurchNeeds.net Team</p>
    </div>
    
    <div class="footer">
      <p>Roaring Lamb Productions<br>
      Questions? Email us at <a href="mailto:support@roaringlambproductions.com">support@roaringlambproductions.com</a></p>
    </div>
  </div>
</body>
</html>
